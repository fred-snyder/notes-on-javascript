// join

var arr = [1, 2, 3];

// join returns a string with all the elements of the array
// it concatenates the elements with the parameter as a separator
console.log(arr.join('+')); // 1+2+3
console.log(arr.join('-')); // 1-2-3
console.log(arr.join()); // 1,2,3
console.log(arr); // [ 1, 2, 3 ]
