// pop (aka remove last)

var arr = [1, 2, 3, 4 ,5]

// pop the last element from the passed array
arr.pop();

console.log(arr) // [1 ,2 ,3, 4]

// array.pop() remove the last element from the array
// and returns the remove element
console.log(arr.pop()) // 4
console.log(arr)// [1, 2, 3]
