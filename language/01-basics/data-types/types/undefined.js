// undefined
var varUndefined = undefined // don't use this to set an empty variable
console.log(null == undefined) // true // they are of the same value
console.log(null === undefined) // false // but not of the same type
