{
  "name": "node-fetch-demo",
  "version": "1.0.0",
  "description": "```bash\r npm i node-fetch\r # flag explainers:\r # -S or --save for dependencies # from npm 5.0.0 modules are added as a dependency by default\r # -D  or --save-dev for development dependencies\r ```",
  "main": "fetch.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.1.0"
  },
  "type": "module"
}
